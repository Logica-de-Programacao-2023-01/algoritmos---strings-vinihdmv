package main

import "fmt"

func main() {
	var str string
	fmt.Print("Escreva um palavra e te diremos ela ao contrario", str)
	fmt.Scanln(&str)
	Aocontra := ""
	for i := len(str) - 1; i >= 0; i-- {
		Aocontra += string(str[i])
	}
	fmt.Println("Ela ao contraio", Aocontra)
}
